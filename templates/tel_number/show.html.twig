{% extends 'base.html.twig' %}

{% block title %}TelNumber{% endblock %}

{% block body %}
    <h1>TelNumber</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tel_number.id }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ tel_number.number }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ tel_number.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('tel_number_index') }}">back to list</a>

    <a href="{{ path('tel_number_edit', {'id': tel_number.id}) }}">edit</a>

    {{ include('tel_number/_delete_form.html.twig') }}
{% endblock %}
